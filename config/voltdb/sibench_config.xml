<?xml version="1.0"?>
<parameters>

    <!-- Connection details -->
    <type>SQLITE</type>
    <driver>org.voltdb.jdbc.Driver</driver>
    <url>jdbc:voltdb://tem51:21212,tem52:21212?jdbc.committhrowexception=false&amp;jdbc.rollbackthrowexception=false</url>
    <username>voltdb</username>
    <password>voltdb</password>
    <batchsize>128</batchsize>
    <newConnectionPerTxn>true</newConnectionPerTxn>

    <scalefactor>1</scalefactor>

    <!-- The workload -->
    <terminals>1</terminals>
    <works>
        <work>
            <time>60</time>
            <rate>unlimited</rate>
            <weights>25,75</weights>
        </work>
    </works>

    <!-- SIBENCH Procedures declaration -->
    <transactiontypes>
        <transactiontype>
            <name>MinRecord</name>
        </transactiontype>
        <transactiontype>
            <name>UpdateRecord</name>
        </transactiontype>
    </transactiontypes>
</parameters>
